import styled from "styled-components";
import PrimaryButton from "../PrimaryButton";

const StripeButton = styled(PrimaryButton)`
  background: #fabc09;
  color: #ffffff;
  a {
    text-decoration: none;
    color: white;
  }
`;

export default StripeButton;
